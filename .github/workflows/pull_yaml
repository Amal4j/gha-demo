name: Create Pull Request Workflow from feature branch
on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'

jobs:
  pullrequest:
    runs-on: ubuntu-latest

    steps:
      - name: Practicing Pull Request
        run: |
          echo "Pull Request Triggered"
        
          echo "hello , {{vars.DEAR_LOVE}}"
