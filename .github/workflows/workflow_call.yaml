# This workflo_call is use as reusable worflow for other Jobs 

name: build stage 
on:
  workflow_call:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Print
        run: echo "Build app"  