name: Create Pull Request Workflow from feature branch
on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'

jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - name: Practicing Psh Request
        run: |
          echo "Push Request Triggered"
        
          echo "hello , ${{vars.DEAR_LOVE}}"
