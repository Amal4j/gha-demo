# learning conditiosn and expresssion

if: $ {{github.ref === 'refs/heads/main'}}

name : practice
on:
  push:
    branches:
      - main
      - 'feature/*'
  pull_request:
jobs:
  condition and expression:
    runs-on: ubuntu-latest
    steps:
      - name: practice
        if: ${{github.ref == 'refs/heads/mmain'}}
      - name: practice1
        if: ${{github.event_name = 'push'}}
      - name: practice2
        if: ${{github.ref == 'refs/heads/main && github.event_name = 'push'}}
        run: echo " Deploying on Main"
      - name: run test
        run: |
          npm ci
          npm run build
          npm test
      - name: sucess message 
        if: ${{ success() }}
        run: echo "its success"  
      
      - name: success
        if: ${{success()}}
      - name: failure 
        if: ${{failure()}}
      - name: independent
        if: ${{always()}} #cleanup / logging / artifact collection 
      - name: cancelled
        if: $ {{cancelled()}}  # workflow cancel hua to setup run karo
        ----------------------

GitHub Context
      ↓
    needs
      ↓
  Your Job ID
      ↓
   GitHub Property
      ↓
    result

---
name: learning npm run build
on:
  push:
    branches:
      - main
      - feature/*
jobs:
  amal_learning:
    runs-on: ubuntu-latest
    steps:
      - name: Amal_Learning_Steps
        run: npm run build
  amal_learning1:
    runs-on: ubuntu-latest
    needs: amal_learning
    steps:
      - name: defining needs and build and results
        if: ${{needs.amal_learning.result == 'success'}} # needs ek context hai and amal_learning ek object/context hai and result ek value hai context ka and success uski property hai
        run: |
          echo "This is ok to run"
-------------------------

name: define env
on:
  push:
    branches:
      - main
      - 'feature/*'
env:
  ENVIRONMENT: Production     
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: deploy_1
        if: ${{env.ENVIRONEMENT == 'Production'}}

        ----------------------

   runs-on: ubuntu-latest
    needs: Build
    steps:
      - name: test
        run: |
          echo "we are deploying app with ${{needs.Build.outputs.tag}}"
----------------------------------------------------

name: job container and service container
jobs:
  job_conatiner:
    runs-on: ubuntu-latest
    container: node: 20
  postgreq:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ vars.POSTGRES_DB }}
        ports:
          - 5432:5432
      